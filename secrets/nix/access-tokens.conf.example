# Nix access tokens for private GitHub repositories
# Usage:
#   cp access-tokens.conf.example access-tokens.conf
#   chmod 600 access-tokens.conf
#   gh auth token  # この出力を下の YOUR_TOKEN に置き換える
#
# Or one-liner:
#   echo "access-tokens = github.com=$(gh auth token)" > access-tokens.conf && chmod 600 access-tokens.conf
access-tokens = github.com=YOUR_TOKEN
