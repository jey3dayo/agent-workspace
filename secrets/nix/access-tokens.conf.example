# Nix access tokens for private GitHub repositories
# Usage:
#   cp access-tokens.conf.example access-tokens.conf
#   chmod 600 access-tokens.conf
#   # Edit access-tokens.conf and replace with your actual token
#
# Token: https://github.com/settings/tokens (Classic, repo scope)
access-tokens = github.com=ghp_YOUR_TOKEN_HERE
